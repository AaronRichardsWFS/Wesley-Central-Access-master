@model IEnumerable<WCAProject.Models.Zinternalcategory>

@{
    ViewData["Title"] = "Index";
}

<h1>View All Internal Subcategories (To be used in "Inquiry" forms)</h1>
<hr/>
<table class="table table-striped">
    <thead class = "thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.internalsubcat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td class = "align-middle">
                @Html.DisplayFor(modelItem => item.internalsubcat)
            </td>
            <td align = "right">
                <a type="button" class="btn btn-warning" asp-action="Edit" asp-route-id="@item.ZinternalcategoryId">Edit</a>
                <a type="button" class="btn btn-secondary" asp-action="Details" asp-route-id="@item.ZinternalcategoryId">View</a>
                <a type="button" class="btn btn-danger" asp-action="Delete" asp-route-id="@item.ZinternalcategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

<div align = "right">
    <a type="button" class="btn btn-success" asp-action="Create">Add Internal Subcategory</a>
    <a class="btn btn-secondary" asp-controller="Home" asp-action="Admin">Back to All Dropdown Content</a>
</div>
