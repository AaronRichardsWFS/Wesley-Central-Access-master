@model WCAProject.Models.Zyesno

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Zyesno</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.yesno)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.yesno)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.ZyesnoId">Edit</a> |
    <a type="button" class="btn btn-secondary" asp-action="Index">Back to List</a>
</div>
